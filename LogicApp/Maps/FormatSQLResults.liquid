{
    "stores": [
        {% assign storeid = '0000' %}
        {% for result in content.ResultSets.Table1 %}
            {%- if forloop.first == false -%}
                {% if result.INVENTORYWAREHOUSEID != storeid %}
                ]      
                },
                {%- endif -%}
            {%- endif -%}
            {% if result.INVENTORYWAREHOUSEID != storeid %}
            { 
                "storeId": "{{ result.INVENTORYWAREHOUSEID }}",
                {% assign storeid = result.INVENTORYWAREHOUSEID %}
                "products": [
            {%- endif -%}
                    {
                    "productId": "{{ result.ITEMNUMBER }}-{{ result.PRODUCTCOLORID }}-{{ result.PRODUCTSIZEID }}{{ result.COMPANY }}",
                    "stockWithBuffer": {{ result.BUFFERQUANTITY }},
                    "stockWithoutBuffer": {{ result.AVAILABLEONHANDQUANTITY }}
                    }{%- if forloop.last == false -%},{%- endif -%}
        {%- if forloop.last == true -%}
                ]
            }
        {%- endif -%}
        {% endfor %} 
    ]
}
